import React from 'react';
import ReactDOM from 'react-dom'; // To render our component in DOM
import App from './App';
ReactDOM.render( <App/>, document.getElementById('root')); // Render 'App' component in the DOM with id 'root'
// from index.html
// The 'root' above comes from index.html file that is located in the public folder

